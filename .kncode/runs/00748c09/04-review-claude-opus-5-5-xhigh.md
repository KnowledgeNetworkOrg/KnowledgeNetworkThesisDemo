REVISE

The change covers all three obligations well. I compared the ported pieces against the design side's current source: the toolbar button, the stop mark, the hover card, the dock's closed rail and the font file all match it line for line. The group rules are right: only stops carry the flag, a group's reading is worked out when needed, and old drafts are converted on load. The pins carry the flag, never merge across it, and it reaches the projector. One real gap remains, in the three-state Optional button the app added itself (the design side designed it only for the toolbar). Under the mouse, "fully on" and "mixed" look exactly the same. That is the exact confusion the obligation's hover rule was written to prevent.

**Must fix:**

1. `src/ds/chrome/PillButton.tsx:106` — the pill picks its hover colour *before* checking whether it is fully on (`hot && !disabled ? t.hoverBg : isOn ? wash : t.bg`). The result: point at a fully-on Optional pill and its fill is swapped for the neutral hover grey, leaving the green ring and ink. That is exactly what a hovered mixed pill shows. So the moment someone points at the button to decide, it can't show whether a press will clear every stop (on) or set every stop (mixed). The card's obligation says "The button tells the truth about a MIXED group", and its hover rule exists because a hover look matching `on` "would appear to change the state". The toolbar port gets this right (`src/ds/chrome/Toolbar.tsx:203` checks `isOn` before hover). The port record's note on the local pill says it is "the same ladder", but it isn't under the mouse. The fix: test `isOn` before `hot`, as the toolbar does. Only the Optional pill passes `selected`, so no other pill changes. Record the extra local difference next to the existing ★ LOCAL entry.

**Optional:**

1. **The card's last "done when" line is not met yet, and nothing here verifies the code.** The build and unit tests haven't run in this run, nor has the browser suite (including the new end-to-end check, which has never run). Nothing has been looked at on the running page, and no receipt or drift-log comment exists. I assume the later check and land steps cover these, but they must not be skipped. The receipt should cover four things:
   - the flag travels as a list alongside the stop ids (the item asks us to name the shape we chose);
   - the real file paths, which the first obligation explicitly asks us to confirm;
   - whether the dock's dashed dot appeared as soon as the stop-mark fix landed;
   - the question about the dark "rail"-style dot in the dock's open row. That question is legitimate: the design side's own dock uses the same style (`WalkDock.jsx:794`).
   
   The drift-log (#74) needs comments on the app-only mixed pill, which also never goes bold for "on" as the toolbar does, and on the font file.
2. **Two different definitions of "this group is optional".** The editor draws a group dashed, and drops it when optionals are bypassed, using only the chosen version's bound stops (`src/state/walk/mockwalk.ts:308`, read at `AuthorRoad.tsx:393` and `:926`). The button reads every stop in every version, unfilled placeholder slots included (`src/state/walk/authordraft.ts:387`). So a fork whose chosen version is all optional draws dashed while the button says mixed. The same happens with an all-optional group holding an unfilled slot. The obligation's wording is a single "every leaf optional". Either line up the two, or say on the receipt why they differ.
3. `tools/studio-spike/browsertest-optionalgroup.mjs:180` presses Escape "to deselect", but nothing in the walk editor handles Escape, so the selection stays. It's harmless to the checks that follow, but the comment is wrong.
4. The port record says the font file was patched by hand and not byte-checked. I compared the design side's current file with ours: every italic declaration and the italic inventory text match. The check step could hash the design side's copy to close that note properly.

Assumptions: The obligations' current text, fetched from the design project today, is the binding reference; I read the amendment block at the head of the italic obligation first. "All four surfaces" means the map pins (including the projector's copy), the dock (open row and closed rail), the walk strip and the hover card. The running-page check, the receipt and the drift-log comments belong to the later steps, not to this edit round. One disclosure: to read the long design-side files I saved two scratch copies to my own temporary folder (`AppData\Local\Temp\ds-sync.md` and `ds-walkdock.jsx`). They are outside the repository, and nothing in the repository was created, edited or deleted.