MINOR

The change does what the card asks. The Reset data button now deletes only a named list of saved entries: the walk draft, the saved walks, leftover positions from the retired floating panel, and the Connections pane's divider width and collapsed flag. A presenter's notes, the categories they created and their deck layout are no longer touched. Both "done when" lines are met. The tooltip now says lecture notes are kept, and the explanation at the top of the stored-data helper file lists every saved entry, which part of the app writes it, and which ones Reset clears. Only the four planned files changed. I checked that nothing else in the app, including the desktop shell, saves to browser storage in a way the new note misses. I also checked that no other test depends on the old tooltip wording.

I traced both tests by hand against the new code. The six unit-test checks pass. In the browser script, two checks would fail against the old code: the lecture-notes check, because the old sweep took them, and the Connections-width check, because the old sweep never reached that entry. So the tests really do guard the fix. The script's "unchanged, byte for byte" check is also sound, because the presenter screen that rewrites lecture entries only exists once someone enters presenter mode, not on an ordinary page load.

Must fix:
none

Optional:
1. **Nothing has actually been run yet.** Neither the full check suite (`npm run verify`) nor the browser script (`npm run test:browser -- reset`) has run in this run. The implementing step was edit-only, and this step may not create files, which the type check's build info, the test cache and the browser script's screenshots all would. My hand trace is not a substitute. Both need to pass before this merges. I did confirm that the "reset" filter selects the reset script.
2. **Both new tests plant the presenter's categories in the wrong shape** (`src/model/storeddata.test.ts:41`, `tools/studio-spike/drive-reset.mjs:84`). The real code saves categories wrapped in an object, `{"list":[...]}` (`src/present/lecturenotes.ts:126`). The fixtures save a bare list, which the real reader would treat as "no categories". This does not weaken what the tests prove today, because Reset only looks at entry names. But if the browser script is ever extended to open presenter mode, the screen would rewrite that entry as `{"list":[]}`. The byte-for-byte check would then fail for a reason unrelated to Reset. Seeding `{ list: [...] }` removes the trap.
3. **The helper's header overstates one Connections entry** (`src/model/storeddata.ts:19-20`). It says the pane also writes a `_narrow` variant in its narrow layout. This app passes the "always split" setting (`src/instruments/ConnectionsPane.tsx:558`), which switches the narrow layout off (`src/ds/connections/ConnectionsSplitPane.tsx:267`). So current code never writes `_narrow` entries; at most they are leftovers from before 2026-08-28. The prefix clears them either way, and the card's "lists what is actually stored" is still met. This is only a matter of precision.
4. **The browser script's opening comment still says "Two questions … both are about what the app does at BOOT"** (`tools/studio-spike/drive-reset.mjs:4`), but it now lists three. The new third one, whether lecture notes are kept, is also one the new unit test answers without a browser.

Noticed outside this card's scope
- `src/instruments/walkdesk/WalkActionBar.tsx:26-27` still says "none of the three stored shapes carries a version field". This change didn't touch that sentence. It is now loose, because there are more than three stored entries, and the lecture ones carry `.v1` in their names.

Assumptions: I treated the unrun suites as a verification gap rather than a code defect, and did not run them because this step forbids creating files. I accepted the plan's reading that "lecture notes are kept" covers all three presenter entries. I also accepted its decision on the question the card left open: the Connections pane's layout entries are cleared, as panel layout.