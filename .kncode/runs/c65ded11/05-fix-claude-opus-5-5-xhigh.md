1. **Fixed.** A new comment in the card's code named the design system's global timer (`window.PKT_SB.LEAVE`), which broke the menu-wash item's rule that searching the card's folder for that name finds nothing. The comment now says the buttons are shown by the helper that tracks whether the pointer is inside the card (`usePresence`), which waits out the shared recede delay (`recedeMs()`). I didn't use the reviewer's suggested `useRecede`, because that helper drives the menu row's ✕, not this condition. Searching the card's folder for the name now finds nothing, so the ledger's "finds nothing" is true again.

2. **Not fixed.** This step only allows editing files, so I couldn't run the checks, the browser tests, or either screenshot driver, including the before-and-after run of the size-check driver (`shot-foldab`) that shows whether the card's geometry really moved. The false claim is corrected: the ledger (the file recording where each ported component came from) said the new tests "proven" the behaviour, and now says they "pin" it, meaning the tests are written to check it, not that they have passed. The re-baseline the card requires is still undone, and the receipt must not claim it until a step that can run commands does it.

**What changed:** one reworded comment in the card's code, which changes no behaviour, and one word in the ledger. I checked that the search now comes back empty and that the ledger still parses as valid JSON. Nothing else was run.

FLAG: unverifiable: This step can only edit files, so who runs `npm run verify`, `npm run test:browser`, and the before-and-after `shot-foldab` / `shot-cardhead` re-baseline the card requires before a receipt can claim it?

Assumptions: I treated the ledger's "Proven … by" wording as part of finding 2, because it claims a test result that doesn't exist yet, and changed only that word. I read "Edit files. That is the only thing you do" as ruling out npm and the drivers, even though the reviewer showed running them would leave the checkout unchanged.